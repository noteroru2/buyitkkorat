---
/**
 * Thin wrapper kept for existing imports — Batch 4 checklist experience.
 */
import ValuationChecklist from "./ValuationChecklist.astro";

interface Props {
  showPrivacy?: boolean;
}

const { showPrivacy = true } = Astro.props;
---

<ValuationChecklist showPrivacy={showPrivacy} />
